# Directives pour l'agent (Claude Code)

Ce fichier est chargé automatiquement à chaque session, y compris en session
web/cloud. Il fixe des règles **opérationnelles** que l'agent doit suivre sans
qu'on ait à les redemander.

## Branches de travail

- **Jamais** livrer le travail sur la branche `claude/<aléatoire>` attribuée par
  défaut à la session. Ce nom est imposé par l'infrastructure, il ne décrit rien.
- Avant d'ouvrir une PR, (re)créer une branche **conforme à la convention du
  dépôt**, dérivée du dernier `origin/main` :
  - `release/X.Y.Z` — un bump de version (lockstep monas).
  - `fix/<slug>` — une correction.
  - `feat/<slug>` — une fonctionnalité.
  - `chore/<slug>` / `refactor/<slug>` — outillage, nettoyage, remaniement.

## Corps des pull requests

- L'intégration GitHub des sessions web **rajoute d'office** un pied de page
  `_Generated by [Claude Code](https://claude.ai/code/session_…)_` au corps des
  PR créées via l'outil MCP — **malgré** `attribution.sessionUrl: false` /
  `attribution.pr: ""` dans `.claude/settings.json` (ces réglages ne sont pas
  honorés pour les PR créées via MCP ; c'est un trou côté plateforme).
- **Toujours** retirer ce pied de page **immédiatement** après
  `create_pull_request` **et** après chaque `update_pull_request` (l'ajout est
  ré-appliqué à chaque écriture du corps) : relire le corps, réécrire sans la
  ligne.
- **Aucun lien de chat/session** (`claude.ai/code/session`, en-tête
  `Claude-Session:`) dans les artefacts poussés : commits, titres/corps de PR,
  code, commentaires. C'est ce lien qui divulgue une session privée.
- En revanche, `Co-Authored-By: Claude …` dans un message de commit est
  **toléré** (attribution d'un binôme) : il ne fuit aucune session.
- Les commits sont **rédigés et signés par le mainteneur** : l'identité git
  (auteur **et** committer) doit être la sienne, jamais « Claude » / une adresse
  d'agent. Un commit signé par la clé du mainteneur mais dont le committer est
  « Claude » apparaît *Unverified* sur GitHub (e-mail ≠ clé de signature).
- Ces trois règles sont tenues **mécaniquement** : hook `.githooks/commit-msg`
  (retire le lien de session) + job CI « Attribution propre » (message, identité,
  corps de PR). Cf. le README racine pour l'activation du hook.

## Commentaires de code et docstrings

- Un commentaire (ou une docstring) explique **le quoi et le pourquoi** de ce
  que fait le code, **brièvement**. Rien de plus.
- **Pas de feuille de route dans le code** : « étape 1/2/3 », « on fera plus
  tard », « cattrs/pydantic viendront ensuite », « pour l'instant »… n'ont pas
  leur place. Ces choix et l'évolution d'une story vivent dans **l'issue** (et
  les ADR liées) ; les dupliquer dans le code les fait diverger.
- Une **référence** suffit pour renvoyer au contexte : `Cf. #12`. Le lecteur
  suit le numéro pour la genèse, la roadmap, les alternatives écartées.
- Même principe pour le `CHANGELOG` : il décrit ce qui **a changé**, pas ce qui
  reste à faire.
